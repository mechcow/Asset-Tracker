<div class="field">
	<label>Role</label><br/><br/>
  <div style="align:right">
  <%= swapselect @asset.class.to_s.underscore.to_sym,@asset, :roles, Role.all.map{ |role| [role.name,role.id] } %>
	<%= link_to image_tag('/images/icons/add.png'), new_role_path, :target => '_blank' %>
	<%= error_message_on :asset, :role_id %>
  </div>
</div>
